<p style="color: green"><%= notice %></p>

<%= render @question %>

<div>
  <%= link_to "Edit", edit_quiz_question_path(@question.quiz, @question) %> |
  <%= link_to "Back to questions", quiz_questions_path(@question.quiz) %>

  <%= button_to "Destroy this question", quiz_question_path(@question.quiz, @question), method: :delete, data: { confirm: "Are you sure?" } %>
</div>
